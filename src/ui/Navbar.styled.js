import styled from 'styled-components';

const NavbarSyled = styled.nav`
  display: flex;
  justify-content: space-between;
  border: 1px black solid;
  padding: 0.8rem;

  img {
    width: 3.5rem;
    height: 2rem;
  }

  button {
    margin: 0.3rem;
  }
`;

export default NavbarSyled;
